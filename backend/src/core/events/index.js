// Événements du Système - Phase 1
// Définitions et typage des événements

const EventTypes = require('./EventTypes');
const EventSchema = require('./EventSchema');
const EventValidator = require('./EventValidator');

module.exports = {
  EventTypes,
  EventSchema,
  EventValidator,
};
