{
  "name": "post:updated",
  "version": "1.0.0",
  "domain": "post",
  "action": "updated",
  "entity": "post",
  "description": "Post updated",
  "schema": {
    "type": "object",
    "properties": {
      "postId": { "type": "string" },
      "userId": { "type": "string" },
      "timestamp": { "type": "string" }
    },
    "required": ["postId", "userId", "timestamp"]
  }
}
