{
  "name": "notifications",
  "version": "1.0.0",
  "description": "Notification delivery, subscriptions, preferences",
  "dependencies": [],
  "capabilities": ["notifications:read", "notifications:send", "notifications:subscribe", "notifications:unsubscribe"],
  "events": ["notification:sent", "notification:read", "subscription:created", "subscription:cancelled"],
  "routes": {
    "prefix": "/api/v1/notifications",
    "methods": ["GET", "POST", "DELETE"]
  },
  "lifecycle": {
    "initRequired": true,
    "readyRequired": true,
    "healthChecks": true
  }
}
