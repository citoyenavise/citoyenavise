{
  "name": "subscription:cancelled",
  "version": "1.0.0",
  "domain": "subscription",
  "action": "cancelled",
  "entity": "subscription",
  "description": "User unsubscribed from notifications",
  "schema": {
    "type": "object",
    "properties": {
      "userId": { "type": "string" },
      "subscriptionType": { "type": "string" },
      "timestamp": { "type": "string" }
    },
    "required": ["userId", "subscriptionType", "timestamp"]
  }
}
