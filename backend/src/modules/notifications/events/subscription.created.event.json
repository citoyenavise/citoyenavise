{
  "name": "subscription:created",
  "version": "1.0.0",
  "domain": "subscription",
  "action": "created",
  "entity": "subscription",
  "description": "User subscribed to notifications",
  "schema": {
    "type": "object",
    "properties": {
      "userId": { "type": "string" },
      "subscriptionType": { "type": "string" },
      "timestamp": { "type": "string" }
    },
    "required": ["userId", "subscriptionType", "timestamp"]
  }
}
