const userRegisteredEvent = require('./user.registered.event.json');
const userLoggedInEvent = require('./user.logged_in.event.json');
const userLoggedOutEvent = require('./user.logged_out.event.json');
const tokenRefreshedEvent = require('./token.refreshed.event.json');

module.exports = {
  userRegisteredEvent,
  userLoggedInEvent,
  userLoggedOutEvent,
  tokenRefreshedEvent,
};
