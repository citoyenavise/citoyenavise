{
  "name": "admin:action_logged",
  "version": "1.0.0",
  "domain": "admin",
  "action": "action_logged",
  "entity": "admin_action",
  "description": "Administrative action logged",
  "schema": {
    "type": "object",
    "properties": {
      "adminId": { "type": "string" },
      "action": { "type": "string" },
      "resourceType": { "type": "string" },
      "timestamp": { "type": "string" }
    },
    "required": ["adminId", "action", "timestamp"]
  }
}
