{
  "name": "admin:audit_event",
  "version": "1.0.0",
  "domain": "admin",
  "action": "audit_event",
  "entity": "audit_log",
  "description": "Audit event recorded",
  "schema": {
    "type": "object",
    "properties": {
      "userId": { "type": "string" },
      "action": { "type": "string" },
      "resource": { "type": "string" },
      "result": { "type": "string" },
      "timestamp": { "type": "string" }
    },
    "required": ["userId", "action", "timestamp"]
  }
}
